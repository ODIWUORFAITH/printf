#include "main.h"
#include <stdio.h>
#include <stdarg.h>
/**
* _printf - The printf function
* @format - The format string with conversion
* Return - The number of characters printed(excluding the null byte used to end output to strings)
*/
int _printf(const char *format, ...)
{
     va_list args;
     int num = 0;

     if (format == NULL)
         return -1;
     va_start(args, format)
     while (*format)
     {
     if (format != '%')
     {
     putchar(*format);
     count++;
     }
     else
     { 
     format++;
     switch(*format)
     {
       

